# Ejercicio básico de CSS

Ejemplo de vinculación entre un documento HTML y un archivo CSS externo.

- `index.html`: estructura HTML y atributos `class`.
- `css/styles.css`: selectores y propiedades visuales.

El archivo CSS contiene más de 15 selectores diferentes y al menos dos propiedades en cada uno.
